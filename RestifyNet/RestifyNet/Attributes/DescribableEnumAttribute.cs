namespace RestifyNet.Attributes;

/// <summary>
/// Enforces the enum to have descriptions for its values
/// </summary>
[AttributeUsage(AttributeTargets.Enum)]
public class DescribableEnumAttribute : Attribute;